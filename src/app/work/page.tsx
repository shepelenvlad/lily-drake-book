import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Gallery — The Dragon Age Gallery | Lily Drake",
	description:
		"Explore the official visual gallery of The Age of Dragons: Awake, My Hero universe, by artists Lily Drake and Shepelenko Bogdan.",
	alternates: {
		canonical: "/work",
	},
};

export default function WorkPage() {
	// Markup lives in <PopupShell />, rendered from the root layout.
	return null;
}
